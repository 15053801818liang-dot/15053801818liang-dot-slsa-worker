{
  "name": "slsa-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Workers + Durable Objects template hardened to SLSA Level 3",
  "main": "src/index.ts",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260808.1",
    "typescript": "^5.0.0",
    "wrangler": "^4.120.0"
  }
}
